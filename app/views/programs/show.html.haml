- content_for :head do
  = tag :link, rel: 'canonical', href: event_program_path(@event, @program)

= render partial: 'control_bar'

.row
  .col-md-12
    %h3
      Supplemental info for
      = @event.name

.row
  .col-md-12
    %p
      - if @program.url.present?
        %b URL
      - else
        %b Download
      %br
      = linked_icon_for_program(@program, link_classes: 'mr-2')
      = @program.name
      - if @program.url.present?
        %br
        %span.fa.fa-fw.mr-2
        = @program.url
.row
  .col-md-12
    %p
      %b Description
      - if @program.description.present?
        = safe_list_sanitizer.sanitize(@program.description).try(:html_safe)
      - else
        %i none

- if can?(:edit, @program)
  .row
    .col-md-12
      %p
        %b Editor's Notes
        %br
        .card
          .card-body
            = safe_list_sanitizer.sanitize(@program.editors_notes).try(:html_safe)

-# There's no social bar here, because only editor can see the Show page
-#.row
-#  .col-md-12
-#    %p= fb_social_bar
