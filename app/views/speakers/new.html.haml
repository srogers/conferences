- title 'Add A New Speaker'

= render partial: 'control_bar'

.row
  .col-md-12
    = simple_form_for @speaker, :html => {:autocomplete => "off"} do |f|
      = render :partial => "form_fields", :locals => {:f => f}
      = save_or_cancel f, speakers_path, 'Create'
