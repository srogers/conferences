.row.mb-2
  .col-md-5.d-none.d-md-block
    = sorting_header current_event_type, :events_path, 'conferences.name'
    = ' / '
    = sorting_header 'Date', :events_path, 'conferences.start_date', icon: 'sort-numeric'
  .col.col-sm-4.d-md-none
    = sorting_header current_event_type, :events_path, 'conferences.name'
    = ' / '
    = sorting_header 'Date', :events_path, 'conferences.start_date', icon: 'sort-numeric'
    = ' / '
    = sorting_header 'Location', :events_path, 'conferences.city,conferences.state'

  .col-md-2.d-none.d-md-block
    -# Desktop gets this
    = sorting_header 'City', :events_path, 'conferences.city'
    = ' / '
    = sorting_header 'State', :events_path, 'conferences.state'

  .col.d-none.d-md-block.text-center
    Info

  - if can? :edit, Conference
    .col.d-none.d-md-block
      Done
  .col-md-1.d-none.d-md-block
    &nbsp;
