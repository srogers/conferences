= render partial: 'control_bar'

.row.chart-controls
  .col-md-12
    = link_to "Done", deduce_done_path, :class => "btn btn-secondary float-right"

    = conference_chart_title('City')

    %p Click a city to see its events
    #cities_chart
      = render partial: 'shared/cities_chart'
    .text-center= current_event_type.pluralize
