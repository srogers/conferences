.row.mb-2
  .col
    = sorting_header 'Name', :publications_path, 'publications.sortable_name'
    = ' / Events'
  .col-md-2
    = 'Format / '
    - if params[:sort].include? 'publications.created_at'
      = sorting_header 'Add', :publications_path, 'publications.created_at', 'sort-numeric'
    - else
      = sorting_header 'Pub', :publications_path, 'publications.published_on', 'sort-numeric'
  .col-md-1.text-right
    = sorting_header 'Mins', :publications_path, 'publications.duration', 'sort-numeric'
  - if params[:heart].present?
    .col-md-2 Needs
  - if can? :edit, Publication
    .col-md-2
