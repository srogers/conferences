// CoffeeScript files are locked into an anonymous function which keeps them out of the global namespace.
// This file defines a global namespace for classes that need to interact. It has to be defined before anything else.
window.CC = window.CC || { };
